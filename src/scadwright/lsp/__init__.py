"""SCADwright Language Server.

Surfaces the equations-DSL parser and class-define-time validators
to LSP-capable editors. The package contains:

- :mod:`.positions` — coordinate-system arithmetic mapping AST node
  positions back to source-file ``(line, column)``.
"""
